# JPEG to WebP Converter

เว็บแอปพลิเคชันสำหรับแปลงไฟล์รูปภาพ JPEG/JPG เป็นรูปแบบ WebP แบบ Client-side ไม่ต้องอัปโหลดไฟล์ขึ้นเซิร์ฟเวอร์

## ✨ ฟีเจอร์

- 🖼️ **แปลง JPEG เป็น WebP** - รองรับไฟล์ .jpg และ .jpeg
- 🎨 **ปรับคุณภาพได้** - ปรับ Quality ตั้งแต่ 1-100%
- 👁️ **Preview แบบ Real-time** - ดูรูปต้นฉบับและผลลัพธ์พร้อมกัน
- 📊 **แสดงขนาดไฟล์** - เปรียบเทียบขนาดก่อนและหลังแปลง พร้อมแสดง % การลดขนาด
- 💾 **ดาวน์โหลดได้ทันที** - ดาวน์โหลดไฟล์ WebP ได้เลยหลังแปลงเสร็จ
- 🖱️ **Drag & Drop** - ลากไฟล์มาวางหรือคลิกเลือกไฟล์
- 🔒 **ปลอดภัย** - ประมวลผลฝั่ง Client ไฟล์ไม่ออกจากเครื่อง

## 🚀 วิธีใช้งาน

### วิธีที่ 1: เปิดไฟล์ตรงๆ
```bash
open index.html
```

### วิธีที่ 2: รัน Local Server (แนะนำ)
```bash
# Python 3
python3 -m http.server 8000

# หรือ Node.js
npx serve

# หรือ PHP
php -S localhost:8000
```

แล้วเปิดเบราว์เซอร์ที่ `http://localhost:8000`

## 📖 ขั้นตอนการใช้งาน

1. **อัปโหลดรูปภาพ**
   - ลากไฟล์ JPEG/JPG มาวางในช่อง Upload
   - หรือคลิกที่ช่องเพื่อเลือกไฟล์

2. **ปรับคุณภาพ (Quality)**
   - เลื่อน Slider เพื่อปรับคุณภาพ (ค่าเริ่มต้น 85%)
   - ค่าสูง = คุณภาพดี แต่ไฟล์ใหญ่
   - ค่าต่ำ = ไฟล์เล็ก แต่คุณภาพลดลง

3. **แปลงไฟล์**
   - กดปุ่ม "แปลงเป็น WebP"
   - รอสักครู่ระหว่างประมวลผล

4. **ดาวน์โหลด**
   - ดู Preview ผลลัพธ์และขนาดไฟล์
   - กดปุ่ม "ดาวน์โหลด WebP" เพื่อบันทึกไฟล์

## 🛠️ เทคโนโลยีที่ใช้

- **HTML5** - โครงสร้างหน้าเว็บ
- **JavaScript** - ประมวลผลแปลงรูปภาพ (Canvas API)
- **HTMX 2.0** - จัดการ interactions
- **Tailwind CSS** - สไตล์ UI แบบ Modern
- **Canvas API** - แปลงรูปภาพฝั่ง Client

## 🌐 รองรับเบราว์เซอร์

- ✅ Chrome/Edge (แนะนำ)
- ✅ Firefox
- ✅ Safari
- ⚠️ Internet Explorer - ไม่รองรับ

## 📂 โครงสร้างไฟล์

```
.
├── index.html          # หน้าเว็บหลัก
└── README.md           # ไฟล์นี้
```

## ⚠️ หมายเหตุ

- รองรับเฉพาะไฟล์ JPEG/JPG เท่านั้น
- ขนาดไฟล์สูงสุดขึ้นอยู่กับหน่วยความจำของเบราว์เซอร์
- แนะนำให้ใช้ Chrome หรือ Edge เพื่อประสิทธิภาพที่ดีที่สุด

## 📝 License

MIT License - ใช้งานได้ฟรี

---

สร้างด้วย ❤️ โดย AI Assistant
